# 更新日志

## v1.0.8 `2026-08-30`
- 配置弹框新增第三种连接模式：用户可自配主流模型 API Key（如 deepseek、glm 等），与原有私服、ARK 官网模式并列
- 界面新增当前使用模型信息展示，私服模式下由服务端返回实际模型，非私服模式展示本地配置模型，让用户明确感知
- 对话报错新增「上报此问题」链接，自动收集错误堆栈并经客服接口上报管理员；未登录私服用户点击时引导先注册
- 每次对话采集 Token 消耗并上报，管理员页新增「Token 流量」Tab，以日/周/月图表展示插件流量，便于分析
- 用户套餐块展示领取的模型类型与剩余数量，并注明来自管理员免费 Token 拆分
- 管理员页新增客服消息清理功能，可按用户或全站删除历史客服消息，避免服务器存储膨胀
- 完善文档：补充「数据」抽屉用途说明与客服通道引导

## v1.0.7 `2026-08-24`
- 提示词重构为「极简索引 `prompt_index` + 5 个领域包 `domain_*` + 5 个知识叶 `knowledge_*` + 3 个执行叶 `execution_*`」的渐进式按需下钻结构，首屏仅注入索引、按场景取子集，缓解长枚举导致的焦点稀释
- 精选工具改为「实现 + `*_SCHEMA` 元数据」紧邻聚合（相似集聚），新增 `semantic_tags`/`source`/`domain` 字段，增删函数只需改一处
- 统一工具入口：`callTool` 作为协议壳派发所有精选工具与 EDA 原生函数，`listTools({scenario})`/`listPrompts({domain})` 分层过滤注入量降至 ~8–12，`searchTools` 增强基于条目自描述 `semantic_tags` 的倒排检索（取代中心登记表）
- MCP 模块重定位：原顶层 `eda-api.js`、`mcp-eda.js`、`mcp-prompt.js` 及 `vendor/*.min.js` 整合至 `iframe/mcp/` 目录（`meta-tools.js`/`curated-tools/`/`prompts.js`/`resources.js`/`eda-api.js`），协议壳与精选工具/提示词/资源职责分离

## v1.0.6 `2026-08-05`
- 新增右侧六栏抽屉面板：图谱、提示词、精选工具、EDA函数、资源、日志，支持一键切换浏览
- 新增知识图谱视图（D3 力导向图），可视化展示提示词与工具的引用网络，支持搜索居中与节点高亮
- AI 生成的代码块支持直接编辑后再执行
- 日志面板 JSON 内容支持按层级折叠展开，结构清晰可读
- 新增导入/导出数据管理，支持从源码导入自定义工具与提示词

## v1.0.5 `2026-07-20`
- 移除嘉立创用户信息自动收集，手动输入用户名

## v1.0.4 `2026-07-14`
- 解决嘉立创假登录的问题
- 扩展重名修复
- 增加使用说明文档

## v1.0.0 `2025-12-26`
- 首次发布：AI巧绘
- 支持 AI 对话与上下文管理，快速解答原理图设计相关问题
- 支持 Function Calling 模式调用 EDA API 读取原理图信息
- 集成火山引擎 ARK API，支持代码生成与执行
- 提供私服模式，方便无 API Key 的用户体验
- 支持双验证方式：AK 验证 / 私服 Token 验证
- 使用 ARK 新 API，提供缓存功能减少 Token 消耗
- 提供 MCP 工具集与优化提示词，使布线更智能并自动进行 RDC 检查
- 支持计算元件边界功能与 SearchTool 自定义 API 覆盖
- 页面按钮逻辑统一管理
- 解决弹框放大缩小时样式混乱问题
- 解决布线压盖问题
- 解决 ToolList 数据量巨大导致 AI 崩溃的问题
- 解决页面发送按钮无响应的问题
- 提供安全确认的写入执行流程
